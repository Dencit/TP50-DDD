<?php

namespace domain\oauth\port\controller;

use think\Request;
use extend\utils\ApiCache;
use domain\base\controller\BaseController;
use domain\base\response\ApiTrans;
use domain\oauth\port\request\OauthTokenRequest;
use domain\oauth\port\logic\OauthTokenLogic;
use domain\oauth\port\trans\OauthTokenTrans;

/**
 * notes: 应用层-控制器
 * 说明: 控制器内不写业务,只写http层面相关的逻辑,
 * 调用原则: 向下调用[输入验证类,业务类,输出转化类].
 */
class OauthTokenController extends BaseController
{

//{@block_c}
//{@block_c/}

//{@block_cj}
//{@block_cj/}

//{@block_bc}
//{@block_bc/}

//{@block_u}
//{@block_u/}

//{@block_bu}
//{@block_bu/}

//{@block_br}
//{@block_br/}

//{@block_r}
//{@block_r/}

//{@block_d}
//{@block_d/}

}